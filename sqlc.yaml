version: "2"
sql:
  - engine: "sqlite"
    queries:
      - "db/sqlc/queries.sql"
    schema: "db/sqlc/schema.sql"
    gen:
      go:
        package: "dbgen"
        out: "internal/storage/dbgen"
        emit_json_tags: true
        emit_interface: true
